# Plan: Add message column to agent dashboard inquiries tab

## Context
The backend `getLeadsForAgent` already returns `message` in its response (line 117 of `leads.service.ts`), but the frontend `Lead` interface doesn't include it, and the inquiries table doesn't render it. The user wants to see the message body in the dashboard.

## Changes

### 1. Add `message` to `Lead` interface
**File:** `apps/web/app/[locale]/(authenticated)/agent/dashboard/AgentDashboardContent.tsx` (line 30-38)

Add `message: string | null` to the interface.

### 2. Add "Message" column to the inquiries table
**File:** `apps/web/app/[locale]/(authenticated)/agent/dashboard/AgentDashboardContent.tsx` (lines 244-274)

- Change grid from `grid-cols-5` to `grid-cols-6`
- Add header: `<div>{t('table.message')}</div>`
- Add row cell: `<div className='text-muted-foreground truncate'>{lead.message || '—'}</div>`

### 3. Add translation keys
**File:** `apps/web/messages/en.json` — add `"message": "Message"` to `agentDashboard.table`
**File:** `apps/web/messages/ar.json` — add `"message": "الرسالة"` to `agentDashboard.table`

## Verification
- Navigate to `/en/agent/dashboard` → click "Inquiries" tab
- Each lead row should show a Message column with the inquiry text
- Hover/truncate long messages gracefully
